package app.cafeteria.repository;

import app.cafeteria.model.CafePersonalizado;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CafePersonalizadoRepository extends JpaRepository<CafePersonalizado, Long> {
}
